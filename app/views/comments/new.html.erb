<%= simple_form_for(@comment) do |com| %>
  <%= com.input :content %>
  <%= com.hidden_field :user_id, value: current_user.id.to_i %>
  <%= com.hidden_field :photo_id, value: photo_id %>
  <%= com.button :submit %>
<% end %>

<%= image_tag @photo.image.url(:medium) %>
  <h3>
    <%= @photo.location %>
  </h3>
  <div>
<button> <%= link_to "Cancel", photos_path %> </button>
<button> <%= link_to "Edit Photo", edit_photo_path(@photo) %> </button>
<button> <%= link_to 'Delete', photo_path(@photo), :method => "delete"%> </button>
  </div>

